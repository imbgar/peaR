# Session 2026-06-03 — MVP build + feature iteration v1.1

Linked from [[MEMORY.md]].

## What I did
- **Phase 1 MVP**: scaffolded the Rust workspace; built `pear-core` (protocol/engine/session/
  github/store/dispatch) + the Tauri `apps/desktop` shell with xterm.js terminals. Core has 9
  tests incl. an end-to-end PTY echo test. App boots clean (real macOS window, no panics).
- **Copy content** reworked to a frontend clipboard action (`tauri-plugin-clipboard-manager`) +
  editable preview modal, instead of relying on `pbcopy` inside the agent.
- **Iteration v1.1** (this session's feature batch):
  - Paste GitHub PR URL to open (`parsePrRef` handles `…/pull/123`).
  - Review tiers Light/Standard/Complex (`StartReview` → `/code-review low|high|ultra`).
  - Auto-review-on-open toggle + tier dropdown (localStorage-persisted).
  - Distill / Walk-thru / Explain / Video buttons + matching `pr-*` skills.
  - Insight panel (collapsible) rendering markdown/diff/note; `LoadPanel` shows the latest saved review.
- Saved the pre-design UI snapshot to `docs/design-snapshots/phase1-v1/` for A/B.
- Wrote `docs/ROADMAP.md` (deferred features DF-1…DF-7 with scope; Insight Bus design) and set
  up this memory structure (mirrors `ephemeral-controller`).

## Key decisions
- **Copy is a frontend action**, not a slash macro — works for any CLI, no `pbcopy` dependency.
- **Insight Bus = file-drop**, not PTY-sentinel scraping (agent CLIs are full-screen TUIs). [ROADMAP DF-3]
- Tiers map to `/code-review` effort levels now; explicit model routing is DF-7.

## What's next
1. `/frontend-design` refactor → new revision; A/B against `phase1-v1` snapshot.
2. DF-6 PR checkout/cwd (so tier reviews target the right repo).
3. DF-3 live Insight Bus (`pear` shim + `notify` watcher).

## Gotchas to remember
- xterm `FitAddon.fit()` must run after the host is visible + on panel toggle / resize.
- Auto-review types the slash command after a 2.2s delay; PTY buffers input so it's safe even if
  the CLI isn't fully booted, but TUI readiness is best-effort.
