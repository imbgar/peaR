{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window + the review-map theater (theater uses events + clipboard; BroadcastChannel does not cross WKWebView windows, so theater<->main messaging rides Tauri events through the Rust core)",
  "windows": [
    "main",
    "review-map"
  ],
  "permissions": [
    "core:default",
    "opener:default",
    "opener:allow-open-url",
    "clipboard-manager:allow-write-text",
    "clipboard-manager:allow-read-text",
    "notification:default",
    "notification:allow-notify",
    "notification:allow-is-permission-granted",
    "notification:allow-request-permission"
  ]
}
